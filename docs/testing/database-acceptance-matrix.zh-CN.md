# 数据库接受矩阵

[English](database-acceptance-matrix.md) · [繁體中文](database-acceptance-matrix.zh-TW.md)

该矩阵保留数据库切换 ID。仅当当前测试满足规定要求时才使用 `executed/pass`。部分源或更窄的测试仍然是 `not run / partial`。

| 身份证号 | 需求总结                                   | 实施证据                                                                                                             | 验证                                                     |
| -------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| DB-01    | 来自一份迁移历史的新数据库                 | Drizzle迁移+模式合约                                                                                                 | 执行/通过：`test:migrations`                             |
| DB-02    | 重复迁移是数据保留无操作                   | `migrateEnvironment` 前缀检查                                                                                        | 执行/通过：`test:migrations`                             |
| DB-03    | 使用目录/绑定升级以前的架构                | 真实的 `0000` 到 `0001` 夹具保留现有的链事件，同时添加可为空的源记录完整性证据；在发布当前摘要之前重新索引重新获取源 | 执行/通过：`test:migrations` 和 Indexer 商店集成         |
| DB-04    | 注入迁移 SQL 失败具有正确的事务/标记边界   | 可注入执行器在真实迁移事务中运行失败的 SQL；部分 DDL 回滚，外部标记保留 `FAILED` 和原始诊断                          | 执行/通过：`test:migrations`                             |
| DB-05    | 检测已更改、丢失或未知的应用历史记录       | 有序历史验证器拒绝被篡改、丢失/落后和未知的账本条目                                                                  | 执行/通过：`test:migrations`                             |
| DB-06    | 没有迁移的架构更改失败存储库检查           | 生成的模式契约绑定排序模式源文件的摘要； `db:check` 拒绝来源/合约漂移                                                | 执行/通过：`test:migrations` 和 `db:check`               |
| DB-07    | 实时缺少索引/约束/列是漂移                 | 标准化模式指纹                                                                                                       | 执行/通过：删除索引测试                                  |
| DB-08    | 启动时模式前/后失败                        | 读取器/写入器使用的运行时验证器拒绝其本机迁移分类账位于代码合约后面的数据库                                          | 执行/通过：`test:migrations` 和运行时读取器边界测试      |
| DB-09    | 迁移后崩溃，标记清除之前恢复，无需重新运行 | `recoverEnvironment`验证                                                                                             | 执行/通过：`test:migrations`                             |
| DB-10    | 大于 2^53 往返的值                         | 规范文本+阅读器                                                                                                      | 执行/通过：`test:db`                                     |
| DB-11    | uint256 最大接受；无效表格被拒绝           | 编解码器 + SQL 检查                                                                                                  | 执行/通过：`test:db`                                     |
| DB-12    | 跨部署的相同销售 ID 是隔离的               | 复合身份                                                                                                             | 执行/通过：`test:db`                                     |
| DB-13    | 原始 SQL 状态无效/FK/重复被拒绝            | SQL 约束/FK/PK                                                                                                       | 执行/通过：`test:db`                                     |
| DB-14    | API无法写入或DDL                           | API仅接收读卡器表面；它的 SQLite 连接是只读的，直接 DDL 尝试被拒绝                                                   | 执行/通过：`test:db` 和架构检查                          |
| DB-15    | 丢失元数据不会阻止摄取，并且 API 标记丢失  | 投影行在没有目录元数据和响应报告 `metadataStatus=MISSING` 的情况下保持可读                                           | 执行/通过：`test:db`                                     |
| DB-16    | 不安全块/索引被拒绝                        | 安全整数检查                                                                                                         | 执行/通过：`test:db`                                     |
| DB-17    | 第二个Indexer无法获取写入器锁              | `flock`写锁                                                                                                          | 执行/通过：`test:db`                                     |
| DB-18    | 运行时阻止迁移/恢复                        | 共享服务门与独占服务门                                                                                               | 执行/通过：`test:db`                                     |
| DB-19    | 维护会阻止新的运行时间                     | 专属门                                                                                                               | 执行/通过：`test:db`                                     |
| DB-20    | 包装器/运行时崩溃正确释放锁                | Harness 拥有的子进程持有运行时/写入器锁，接收 `SIGKILL`，关闭其生命周期管道，并且新所有者获取两个锁                  | 执行/通过：`test:db`                                     |
| DB-21    | 崩溃标记阻止运行时间                       | 外部标记检查                                                                                                         | 执行/通过：`test:db`                                     |
| DB-22    | 繁忙超时/磁盘错误失败但不跳过工作          | 受控`SQLITE_BUSY`保留阻塞值；通过 `max_page_count` 有界 `SQLITE_FULL` 回滚整个事务                                   | 执行/通过：`test:db`；未声明主机文件系统耗尽             |
| DB-23    | 相同目录种子重新运行是无操作的             | 交易平等检查                                                                                                         | 执行/通过：`test:seeds`                                  |
| DB-24    | 手动目录编辑不会被覆盖                     | `SEED_CONFLICT` 并回滚                                                                                               | 执行/通过：`test:seeds`                                  |
| DB-25    | 跨部署的相同令牌独立绑定                   | 复合结合PK                                                                                                           | 执行/通过：`test:seeds`                                  |
| DB-26    | 已完成的链种子不会重新发送                 | Bootstrap 将每个链步骤保留为 `PREPARED`、`SUBMITTED`，然后是 `VERIFIED`；完成重新运行验证并重用现有清单              | 执行/通过：日志单元案例和实际堆栈重新运行                |
| DB-27    | 广播中断已验证或未知                       | 日志恢复已知提交的哈希值，重新验证已验证的收据，并针对不明确的提交或搁浅的 `PREPARED` 步骤停止 `UNKNOWN`             | 对于确定性状态机情况执行/通过；真正的进程终止窗口未运行  |
| DB-28    | 重新运行引导程序不会反转用户操作           | 真实堆栈测试完成用户结算和恢复操作，重新运行引导程序，并断言块高度、计数器、所有者、清单、日志和投影不变             | 执行/通过：真实堆栈重新运行                              |
| DB-29    | 种子版本/清单不匹配停止                    | 应用的目录种子版本 `1` 拒绝版本 `2` 和 `SEED_CONFLICT` 并保留原始数据集                                              | 执行/通过：`test:seeds`                                  |
| DB-30    | Bootstrap 通过目标块/哈希断言完成          | Bootstrap捕获目标，追上，验证规范哈希，写入收据；真实堆栈设置和重新运行练习都创建和重用路径                          | 执行/通过：真实堆栈引导并重新运行                        |
| DB-31    | 演示状态来自真实事件                       | 真实的 Anvil 生命周期场景已通过                                                                                      | 执行/通过：真实堆栈生命周期                              |
| DB-32    | Event/投影/header/checkpoint一起回滚       | SQLite适配器事务回滚测试                                                                                             | 执行/通过：Indexer 商店集成                              |
| DB-33    | 零事件块推进完整的检查点                   | 使用检查点提交的已完成块记录和摘要                                                                                   | 执行/通过：Indexer 商店集成                              |
| DB-34    | 同块重放是经过内容验证的无操作             | 适配器验证存储的事件/块内容                                                                                          | 执行/通过：Indexer 商店集成                              |
| DB-35    | 相同的事件标识/不同的内容是完整性错误      | 适配器拒绝相互矛盾的原始证据                                                                                         | 执行/通过：Indexer 商店集成                              |
| DB-36    | 新发现的登录已完成的旧块需要重新索引       | 已完成的块内容摘要拒绝新发现的带有 `COMPLETED_BLOCK_CONTENT_MISMATCH` 的日志                                         | 执行/通过：Indexer 商店集成                              |
| DB-37    | 投影器版本不匹配会阻止增量摄取             | 商店启动比较持久化的投影器合约并拒绝与 `PROJECTION_CONTRACT_MISMATCH` 不匹配                                         | 执行/通过：Indexer 商店集成                              |
| DB-38    | 损坏的投影从所有原始事件重建               | 原子本地-日志重建                                                                                                    | 执行/通过：Indexer 商店集成                              |
| DB-39    | 失败的重建永远不会暴露半构建；目录已保存   | 重建事务和目录保存断言                                                                                               | 执行/通过：Indexer 商店集成                              |
| DB-40    | Reorg reindex 产生规范结果和可追溯的旧证据 | 真正的Anvil快照/恢复孤儿资助事件；重新索引恢复规范的 `LISTED` 状态，同时保留非规范块下的原始事件                     | 执行/通过：真正的数据库恢复集成                          |
| DB-41    | 检测到重用链/地址的 Anvil 复位             | 在 `anvil_reset` 之后，确定性地址在新的部署 ID 下重新部署，并且旧环境拒绝使用 `DEPLOYMENT_MISMATCH` 重新索引         | 执行/通过：真正的数据库恢复集成                          |
| DB-42    | 除了操作字段之外，两个重建在语义上是相同的 | 真实堆栈测试根据相同的经过验证的证据重建两次，并比较销售和车辆结果                                                   | 执行/通过：实栈恢复                                      |
| DB-43    | 在 H 处比较，而头 H+k 给出 MATCH + LAGGING | 真实堆栈测试挖掘稍后的头块并在锚定检查点断言 `MATCH` 和 `PROJECTION_LAGGING`                                         | 执行/通过：实栈恢复                                      |
| DB-44    | 通过完整性范围检测缺失的整个销售           | 协调器锚定 `saleCount` 并检查 `[1, saleCount]` 中的每个销售 ID                                                       | 执行/通过：实栈恢复                                      |
| DB-45    | 缺少历史状态/更改锚点是无法验证的          | Anvil快照/恢复删除索引锚点；协调器持续存在 `UNVERIFIABLE` 错误并退出非零                                             | 执行/通过：实栈恢复                                      |
| DB-46    | 后来的 NFT 转移推动了当前所有者的比较      | 协调器通过锚定的 `nextTokenId` 枚举每个铸造的代币，比较 `ownerOf`，并检测完全丢失的所有权行                          | 执行/通过：实栈恢复                                      |
| DB-47    | WAL 提交的数据在独立快照恢复中仍然存在     | SQLite 备份 API                                                                                                      | 执行/通过：`test:recovery`                               |
| DB-48    | 损坏的校验和/元数据在主动突变之前被拒绝    | 备份验证器                                                                                                           | 执行/通过：校验和情况                                    |
| DB-49    | 头部后面恢复的相同部署可以赶上             | 一致的预资助备份恢复旧的检查点，相同的 Indexer 赶上未更改的实时部署，`FUNDED` 投影                                   | 执行/通过：真正的数据库恢复集成                          |
| DB-50    | 恢复文件切换崩溃是可诊断/可恢复的          | 标记恢复隔离主/WAL/SHM 代                                                                                            | 执行/通过：杀死热 WAL 开关；未声明硬件断电               |
| DB-51    | 重置拒绝外部节点和主动引导程序所有权       | Reset 拒绝错误的 RPC 身份和 `anvil_reset` 之前的引导锁                                                               | 执行/通过：重置 CLI 生命周期矩阵；环回模拟未在本地声明。 |
| DB-52    | 重置保留锁定 inode 和备份                  | 重置测试保留所有者标记、锁定索引节点和备份，同时删除生成的运行时状态                                                 | 执行/通过：`test:db`                                     |
| DB-53    | API数据/来源共享一读快照                   | 第二个写入者在读屏障处提交完成；飞行中的观察仍然返回早期的销售、检查点和活动快照                                     | 执行/通过：Indexer 商店集成                              |
| DB-54    | 并行套件隔离数据库/链/随机数/端口/锁       | 真实堆栈、事务融合和数据库恢复套件使用单独的根、数据库、端口、锁、部署和签名者随机数空间                             | 执行/通过：三个真实套件与`--maxWorkers=3`同时运行        |

当前执行的数据库记录在`docs/evidence/verification.json`中。该矩阵不会将较窄的测试转换为完整项目的测试。
| DB-55 | 中断的迁移仅在记录的捆绑包标识下恢复或完成 | 失败的已知前缀仍然需要执行操作，不匹配的捆绑包将被拒绝，匹配的迁移将继续，并且当前的 SQL 在标记删除之前最终确定 `db_contract` | 执行/通过：`test:migrations` |
| DB-56 | 源不完整重建仅转换为完整的身份绑定重新索引 | 拒绝选择加入和不匹配的扫描开始；显式扫描开始转换获取新的操作 ID 并记录失败的重建沿袭 | 执行/通过：投影维护测试 |
| DB-57 | 迁移备份保留有效的预部署数据库 | 零部署且无部署 sidecar 的真正旧模式数据库可生成经过验证的 `PREDEPLOYMENT` 备份、保留目录数据并在无需伪造链身份的情况下进行迁移 | 执行/通过：`test:migrations` 和 `test:recovery`；在主动替换之前直接恢复被拒绝 |
| DB-58 | 托管本地重置仅限于一个专用节点 | Bootstrap 自动声明一个标准化的环回端点；索引器启动和重置验证绑定；重置会保留它；单位固定装置涵盖别名、冲突、变更和所有权缺失 | 执行/通过：数据库单元测试和Anvil `managed-node-reset` 的两个集成；未知进程和转发别名仍然在声明模型之外 |
| DB-59 | 哈希后种子日志写入故障恢复，无需重新提交 | 一次注入的 `SUBMITTED` 写入失败将被重试，作为持久的已知哈希证据；重新打开的日志验证哈希值，同时提交计数仍然为 1 | 执行/通过：链种子日志单元回归 |
| DB-60 | 第一次对账磁头故障创建持久的故障报告 | 真实内存中的 SQLite 收到新的 `UNVERIFIABLE / HEAD_UNKNOWN` 报告，其中包含检查点身份和 RPC 原因； writer 关闭且退出状态非零 | 执行/通过：对账报告生命周期回归 |
| DB-61 | 对账保留 NFT 收藏身份 | 清单集合行满足锚定所有权；错误集合行是显式的 `UNEXPECTED_COLLECTION` 不匹配，即使在正确行旁边也是如此；其他部署保持隔离 | 执行/通过：重点SQLite/CLI案例和真实Anvil/SQLite 对账 |
| DB-62 | 重新获取之前将矛盾的源内容存档 | 非空事件/标头摘要不匹配需要经过验证的完整源刷新；备份保留旧证据，重新获取活动链源，保留目录；纯缺失行使用倒回/重新插入 | 执行/传递：Indexer存储和真实Anvil恢复集成 |
| DB-63 | 在 SQL 之前重试失败的迁移前备份 | 匹配没有备份证明的失败标记会重新运行真正的备份验证，并在重复失败时保持模式/历史不变 | 执行/通过：`test:migrations` |
| DB-64 | 已完成的迁移前备份被重新验证并重复使用 | 标记保留已发布的备份 ID；匹配简历验证源/部署/架构证据，并在注入 SQL 失败后使用一个快照 | 执行/通过：`test:migrations` |
| DB-65 | 不采用现有的无主数据库 | 初始化程序和迁移在所有者创建之前拒绝并保留原始数据库字节 | 执行/通过：`test:migrations` |
| DB-66 | 不采用现有的无主 sidecar | 部署、引导接收、种子日志、维护和托管节点 sidecar 保持字节相同，无需所有者元数据 | 执行/通过：`test:migrations` |
| DB-67 | 空环境所有权仍然是幂等的 | 空目录接收一条所有者记录而不创建数据库；重新运行保留所有者字节 | 执行/通过：`test:migrations` |

| DB-68 | 孤立标记临时文件不会束缚匹配的简历 | 一个真正的孩子冲掉了以前的固定名称临时标记并被杀死；发布的 `PREPARED` 标记仍然具有权威性，相同的操作运行一次，孤儿被删除 | 执行/通过：`test:db` |
| DB-69 | 标准备份拒绝不完整的维护源 | 失败的重新索引标记保持字节相同，并且不会发布备份包 | 执行/通过：`test:recovery` |
| DB-70 | 维护档案保留原始状态，无法正常恢复 | 一次源刷新归档记录操作且投影证据为`EVIDENCE_ONLY`；验证成功并恢复隔离前拒绝 | 执行/通过：`test:recovery` |
| DB-71 | 备份和恢复不能跨越不完整的引导生成 | 共享/独占引导程序所有权块重叠；不完整或更改的种子日志/收据证据在备份发布或恢复隔离之前被拒绝 | 执行/通过：恢复和迁移测试 |
| DB-72 | 对账新鲜度使用发表时间头 | 受控头部在锚定比较期间前进并产生 `MATCH / PROJECTION_LAGGING`；出版头故障产生耐用的 `UNVERIFIABLE / HEAD_UNKNOWN` | 执行/通过：对账生命周期集成 |
| DB-73 | 重置意图先于链突变，恢复使用重置阶段证据 | `RESET / PREPARED`在链回调之前是持久的；进程死亡仍然可操作； `CHAIN_RESET` 恢复完成生成状态清理而不祝福旧数据库 | 已执行/通过：重置数据库、CLI、child-kill 和托管节点集成测试 |
| DB-74 | 重置拒绝在链突变之前替换破坏性子路径 | 维护所有权后，`databaseDir` 和 `reportsDir` 必须保留真实环境根目录包含的非符号链接目录；用外部符号链接替换会使链回调、标记和外部字节保持不变 | 执行/通过：`test:db` 中的真实临时文件系统和符号链接回归 |
| DB-75 | 恢复在活动隔离之前重新验证复制的快照字节 | 复制后，暂存数据库 SHA-256 仍必须与验证的备份清单匹配；在活动主/WAL/SHM 移动之前验证/复制源更改失败 | 执行/通过：`test:recovery`中真实的better-sqlite3备份/恢复回归 |

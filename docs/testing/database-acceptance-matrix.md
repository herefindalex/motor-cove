# Database acceptance matrix

This matrix preserves the database handoff IDs. `executed/pass` is used only when the current test
covers the stated requirement. Partial source or narrower tests remain `not run / partial`.

| ID    | Requirement summary                                                    | Implementation evidence                                                                                                                                                             | Verification                                                                           |
| ----- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| DB-01 | Fresh DB from one migration history                                    | Drizzle migration + schema contract                                                                                                                                                 | executed/pass: `test:migrations`                                                       |
| DB-02 | Repeat migration is data-preserving no-op                              | `migrateEnvironment` prefix check                                                                                                                                                   | executed/pass: `test:migrations`                                                       |
| DB-03 | Upgrade previous schema with catalog/bindings                          | Real `0000` to `0001` fixture preserves an existing chain event while adding nullable source-record integrity evidence; reindex reacquires source before publishing current digests | executed/pass: `test:migrations` and Indexer store integration                         |
| DB-04 | Injected migration SQL failure has correct transaction/marker boundary | Injectable executor runs failing SQL inside the real migration transaction; partial DDL rolls back and the external marker remains `FAILED` with the original diagnostic            | executed/pass: `test:migrations`                                                       |
| DB-05 | Detect changed, missing, or unknown applied history                    | Ordered history verifier rejects tampered, missing/behind, and unknown ledger entries                                                                                               | executed/pass: `test:migrations`                                                       |
| DB-06 | Schema change without migration fails repository check                 | Generated schema contract binds a digest of sorted schema source files; `db:check` rejects source/contract drift                                                                    | executed/pass: `test:migrations` and `db:check`                                        |
| DB-07 | Live missing index/constraint/column is drift                          | Normalized schema fingerprint                                                                                                                                                       | executed/pass: dropped-index test                                                      |
| DB-08 | Schema ahead/behind fails at startup                                   | Runtime verifier used by readers/writers rejects a database whose native migration ledger is behind the code contract                                                               | executed/pass: `test:migrations` and runtime-reader boundary test                      |
| DB-09 | Crash after migration before marker clear recovers without rerun       | `recoverEnvironment` verification                                                                                                                                                   | executed/pass: `test:migrations`                                                       |
| DB-10 | Values above 2^53 round-trip                                           | Canonical text + reader                                                                                                                                                             | executed/pass: `test:db`                                                               |
| DB-11 | uint256 max accepted; invalid forms rejected                           | Codec + SQL CHECK                                                                                                                                                                   | executed/pass: `test:db`                                                               |
| DB-12 | Same sale ID across deployments is isolated                            | Composite identity                                                                                                                                                                  | executed/pass: `test:db`                                                               |
| DB-13 | Raw SQL invalid status/FK/duplicate rejected                           | SQL constraints/FKs/PK                                                                                                                                                              | executed/pass: `test:db`                                                               |
| DB-14 | API cannot write or DDL                                                | API receives only the reader surface; its SQLite connection is read-only and a direct DDL attempt is rejected                                                                       | executed/pass: `test:db` and architecture check                                        |
| DB-15 | Missing metadata does not block ingestion and API marks missing        | Projection rows remain readable without catalog metadata and the response reports `metadataStatus=MISSING`                                                                          | executed/pass: `test:db`                                                               |
| DB-16 | Unsafe block/index rejected                                            | safe integer CHECK                                                                                                                                                                  | executed/pass: `test:db`                                                               |
| DB-17 | Second Indexer cannot acquire writer lock                              | `flock` writer lock                                                                                                                                                                 | executed/pass: `test:db`                                                               |
| DB-18 | Runtime blocks migration/restore                                       | Shared vs exclusive service gate                                                                                                                                                    | executed/pass: `test:db`                                                               |
| DB-19 | Maintenance blocks new runtime                                         | Exclusive gate                                                                                                                                                                      | executed/pass: `test:db`                                                               |
| DB-20 | Wrapper/runtime crash releases lock correctly                          | Harness-owned child holds runtime/writer locks, receives `SIGKILL`, closes its lifetime pipe, and a new owner acquires both locks                                                   | executed/pass: `test:db`                                                               |
| DB-21 | Crash marker blocks runtime                                            | External marker check                                                                                                                                                               | executed/pass: `test:db`                                                               |
| DB-22 | Busy timeout/disk error fails without skipping work                    | Controlled `SQLITE_BUSY` preserves the blocked value; bounded `SQLITE_FULL` via `max_page_count` rolls back the whole transaction                                                   | executed/pass: `test:db`; host-filesystem exhaustion not claimed                       |
| DB-23 | Same catalog seed rerun is no-op                                       | Transactional equality check                                                                                                                                                        | executed/pass: `test:seeds`                                                            |
| DB-24 | Manual catalog edit is not overwritten                                 | `SEED_CONFLICT` and rollback                                                                                                                                                        | executed/pass: `test:seeds`                                                            |
| DB-25 | Same token across deployments binds independently                      | Composite binding PK                                                                                                                                                                | executed/pass: `test:seeds`                                                            |
| DB-26 | Completed chain seed does not resend                                   | Bootstrap persists each chain step as `PREPARED`, `SUBMITTED`, then `VERIFIED`; completed rerun verifies and reuses the existing manifest                                           | executed/pass: journal unit cases and real-stack rerun                                 |
| DB-27 | Broadcast interruption becomes verified or UNKNOWN                     | Journal resumes a known submitted hash, revalidates verified receipts, and stops `UNKNOWN` for an ambiguous submit or stranded `PREPARED` step                                      | executed/pass for deterministic state-machine cases; real process-kill window not run  |
| DB-28 | Bootstrap rerun does not reverse user actions                          | Real-stack test completes user settlement and recovery actions, reruns bootstrap, and asserts unchanged block height, counters, owner, manifest, journal, and projections           | executed/pass: real-stack rerun                                                        |
| DB-29 | Seed version/manifest mismatch stops                                   | Applied catalog seed version `1` rejects version `2` with `SEED_CONFLICT` and preserves the original dataset                                                                        | executed/pass: `test:seeds`                                                            |
| DB-30 | Bootstrap completes by target block/hash assertions                    | Bootstrap captures target, catches up, verifies canonical hash, writes receipt; real-stack setup and rerun exercise both create and reuse paths                                     | executed/pass: real-stack bootstrap and rerun                                          |
| DB-31 | Demo state comes from real events                                      | Real Anvil lifecycle scenario passed                                                                                                                                                | executed/pass: real-stack lifecycle                                                    |
| DB-32 | Event/projection/header/checkpoint rollback together                   | SQLite adapter transaction rollback test                                                                                                                                            | executed/pass: Indexer store integration                                               |
| DB-33 | Zero-event block advances complete checkpoint                          | Completed-block record and digest committed with checkpoint                                                                                                                         | executed/pass: Indexer store integration                                               |
| DB-34 | Same-block replay is content-verified no-op                            | Adapter verifies stored event/block contents                                                                                                                                        | executed/pass: Indexer store integration                                               |
| DB-35 | Same event identity/different content is integrity error               | Adapter rejects conflicting raw evidence                                                                                                                                            | executed/pass: Indexer store integration                                               |
| DB-36 | Newly discovered log in completed old block requires reindex           | A completed-block content digest rejects a newly discovered log with `COMPLETED_BLOCK_CONTENT_MISMATCH`                                                                             | executed/pass: Indexer store integration                                               |
| DB-37 | Projector version mismatch blocks incremental ingestion                | Store startup compares the persisted projector contract and rejects mismatch with `PROJECTION_CONTRACT_MISMATCH`                                                                    | executed/pass: Indexer store integration                                               |
| DB-38 | Damaged projection rebuilds from all raw events                        | Atomic local-journal rebuild                                                                                                                                                        | executed/pass: Indexer store integration                                               |
| DB-39 | Failed rebuild never exposes half-build; catalog preserved             | Rebuild transaction and catalog preservation assertions                                                                                                                             | executed/pass: Indexer store integration                                               |
| DB-40 | Reorg reindex yields canonical result and traceable old evidence       | Real Anvil snapshot/revert orphans a funding event; reindex restores canonical `LISTED` state while retaining the raw event under a noncanonical block                              | executed/pass: real database recovery integration                                      |
| DB-41 | Anvil reset with reused chain/address is detected                      | After `anvil_reset`, deterministic addresses are redeployed under a new deployment ID and the old environment refuses reindex with `DEPLOYMENT_MISMATCH`                            | executed/pass: real database recovery integration                                      |
| DB-42 | Two rebuilds are semantically equal except operation fields            | Real-stack test rebuilds twice from the same verified evidence and compares sale and vehicle results                                                                                | executed/pass: real-stack recovery                                                     |
| DB-43 | Compare at H while head H+k gives MATCH + LAGGING                      | Real-stack test mines a later head block and asserts `MATCH` plus `PROJECTION_LAGGING` at the anchored checkpoint                                                                   | executed/pass: real-stack recovery                                                     |
| DB-44 | Missing entire sale is detected through completeness scope             | Reconciler anchors `saleCount` and checks every sale ID in `[1, saleCount]`                                                                                                         | executed/pass: real-stack recovery                                                     |
| DB-45 | Missing historical state/changed anchor is UNVERIFIABLE                | Anvil snapshot/revert removes the indexed anchor; reconciler persists `UNVERIFIABLE` with an error and exits nonzero                                                                | executed/pass: real-stack recovery                                                     |
| DB-46 | Later NFT transfer drives current-owner comparison                     | Reconciler enumerates every minted token through anchored `nextTokenId`, compares `ownerOf`, and detects a wholly missing ownership row                                             | executed/pass: real-stack recovery                                                     |
| DB-47 | WAL committed data survives standalone snapshot restore                | SQLite backup API                                                                                                                                                                   | executed/pass: `test:recovery`                                                         |
| DB-48 | Corrupt checksum/metadata rejected before active mutation              | Backup verifier                                                                                                                                                                     | executed/pass: checksum case                                                           |
| DB-49 | Same deployment restored behind head can catch up                      | A consistent pre-funding backup restores the old checkpoint and the same Indexer catches up to the unchanged live deployment and `FUNDED` projection                                | executed/pass: real database recovery integration                                      |
| DB-50 | Restore file-switch crash is diagnosable/recoverable                   | Marker recovery isolates main/WAL/SHM generations                                                                                                                                   | executed/pass: killed hot WAL switch; hardware power loss not claimed                  |
| DB-51 | Reset refuses external nodes and active bootstrap ownership            | Reset refuses wrong RPC identity and bootstrap lock before `anvil_reset`                                                                                                            | executed/pass: reset CLI lifecycle matrix; loopback impersonation not claimed locally. |
| DB-52 | Reset preserves lock inode and backups                                 | Reset test preserves owner marker, lock inodes, and backups while removing generated runtime state                                                                                  | executed/pass: `test:db`                                                               |
| DB-53 | API data/provenance share one read snapshot                            | A second writer commits completion at a read barrier; the in-flight observation still returns the earlier sale, checkpoint, and event snapshot                                      | executed/pass: Indexer store integration                                               |
| DB-54 | Parallel suites isolate DB/chain/nonces/ports/locks                    | Real-stack, transaction convergence, and database recovery suites use separate roots, databases, ports, locks, deployments, and signer nonce spaces                                 | executed/pass: three real suites run concurrently with `--maxWorkers=3`                |

The current executed database records are in `docs/evidence/verification.json`. This matrix does not
convert narrower tests into full-item passes.
| DB-55 | Interrupted migration resumes or finalizes only under the recorded bundle identity | A failed known prefix remains action-required, a mismatched bundle is rejected, the matching migration continues, and current SQL finalizes `db_contract` before marker removal | executed/pass: `test:migrations` |
| DB-56 | Source-incomplete rebuild transitions only to a full identity-bound reindex | No opt-in and a mismatched scan start are rejected; an explicit scan-start transition gets a new operation ID and records failed rebuild lineage | executed/pass: projection maintenance tests |
| DB-57 | Migration backup preserves a valid predeployment database | A real old-schema database with zero deployments and no deployment sidecars produces a verified `PREDEPLOYMENT` backup, preserves catalog data, and migrates without fabricated chain identity | executed/pass: `test:migrations` and `test:recovery`; direct restore is rejected before active replacement |
| DB-58 | Managed local reset is scoped to one dedicated node | Bootstrap atomically claims a normalized loopback endpoint; indexer startup and reset verify the binding; reset preserves it; unit fixtures cover aliases, conflicts, changes, and missing ownership | executed/pass: database unit tests and the two-Anvil `managed-node-reset` integration; unknown process and forwarding aliases remain outside the claim model |
| DB-59 | Post-hash seed journal write fault resumes without resubmission | One injected `SUBMITTED` write failure is retried as durable known-hash evidence; a reopened journal verifies that hash while submit count remains one | executed/pass: chain-seed journal unit regression |
| DB-60 | First reconciliation head failure creates a durable failed report | Real in-memory SQLite receives a new `UNVERIFIABLE / HEAD_UNKNOWN` report with checkpoint identity and RPC cause; writer closes and exit status is nonzero | executed/pass: reconciliation report lifecycle regression |

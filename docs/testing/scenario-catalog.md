# Scenario catalog

This catalog preserves the stable scenario IDs from the implementation brief and links observable
behavior to current local evidence. A passing row describes the recorded run in
[verification.json](../evidence/verification.json); it is not a permanent claim about later source.

| ID       | Observable behavior                                                                                      | Current evidence                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SALE-001 | Approve and list; custody begins only when `createSale` succeeds                                         | Foundry and Playwright local-chain scenarios passed.                                                                                                                                                                                                                                                                                                                                                                                                   |
| SALE-002 | A non-seller sees and funds the exact wei price, and the exact event converges                           | Foundry, bigint formatter, component action, API contract, real-stack, transaction-convergence, and Playwright scenarios passed.                                                                                                                                                                                                                                                                                                                       |
| SALE-003 | Complete, transfer the NFT, then withdraw seller proceeds separately                                     | Foundry, real-stack, and Playwright scenarios passed.                                                                                                                                                                                                                                                                                                                                                                                                  |
| SALE-004 | Cancel a listed sale and reclaim the NFT separately                                                      | Foundry cancellation and NFT-reclaim cases passed.                                                                                                                                                                                                                                                                                                                                                                                                     |
| SALE-005 | Expire, withdraw the buyer refund, and reclaim the NFT                                                   | Foundry, real-stack, and Playwright scenarios passed.                                                                                                                                                                                                                                                                                                                                                                                                  |
| SALE-006 | Direct safe and unsafe NFT deposits cannot create untracked escrow custody                               | Foundry unit/invariant and fresh-Anvil bootstrap binding tests passed.                                                                                                                                                                                                                                                                                                                                                                                 |
| TX-001   | Rejection, unknown submission, receipt, replacement, and projection remain distinct                      | Durable pre-write and storage-failure tests passed. Real Anvil tests classified fee-bump replacement as `REPRICED`, a self-send as `CANCELLED`, and a different call as `DIFFERENT_CALL`. Playwright broadcast once, lost the EIP-1193 response with error 4900, then recovered from a supplied hash.                                                                                                                                                  |
| TX-002   | Reload and account/network/deployment changes preserve original transaction context without resubmission | Controlled promise tests prove slow verification nonce results cannot erase newer evidence. A pending simulation cannot open the wallet after deployment identity changes. Candidate inspection verifies saved descriptor, RPC chain ID, and escrow deployment identity before transaction lookup. Client-side product navigation preserves a volatile known hash without another wallet request; a real reload keeps the documented durability limit. |
| TX-003   | Wallet request outcome survives concurrent journal recovery without replacing transaction evidence       | Unit and browser-storage integration cases cover hashless observer revision races, candidate-hash preservation, reload durability, storage-write failure during read-only verification, and volatile-only warnings.                                                                                                                                                                                                                                    |
| TX-004   | Durable storage failure preserves visible tab-local transaction evidence                                 | Browser adapter, component, and Playwright tests force storage access or write denial, retain volatile evidence, preserve the last durable bytes, expose `STORAGE_UNAVAILABLE`, and show the reload limitation without opening another wallet request.                                                                                                                                                                                                 |
| TX-005   | One in-flight immutable intent owns the wallet request boundary                                          | Capability and React component regressions hold simulation pending, activate the same Sale action twice, and observe one journal entry, one simulation, one wallet request, and a disabled busy control; unrelated intents remain independent.                                                                                                                                                                                                         |
| TX-006   | One browser-profile observer owns each operation verification workflow                                   | Coordinator and component tests prove busy automatic polls skip, manual checks queue, the latest revision is reloaded after ownership, and rejection releases ownership. Two real Chromium pages verify cross-tab exclusion, owner-close handoff, independent operation keys, and unrelated journal writes.                                                                                                                                            |
| TX-007   | One browser-profile submitter owns each immutable intent and volatile hashes promote safely              | Capability and coordinator tests reject same-intent ownership before journal, simulation, or wallet work. Journal tests promote unique volatile transaction evidence over a concurrent hashless durable revision and reject conflicting evidence. Two real Chromium pages verify one same-intent wallet request, independent intents, normal release, and owner-close handoff.                                                                         |
| TX-008   | A returned hash retains durable same-intent ownership through reload until a proved outcome              | Capability regression tests reject sequential pending submissions without a second wallet write, preserve independent intents, allow a fresh attempt after wallet rejection, and link explicit retries after a reverted receipt.                                                                                                                                                                                                                       |
| TX-009   | A nonce-backed original hash that disappears requires a verified wallet replacement hash                 | Chain-reader, recovery, and Observer regressions persist `REPLACEMENT_HASH_REQUIRED`, retain the original intent and hash, request a wallet activity hash, and perform only read-only verification without resubmission.                                                                                                                                                                                                                               |
| IDX-001  | A stopped or transport-limited Indexer exposes stale observation or lag without inventing freshness      | Clock-controlled reader tests separate last-known status from heartbeat freshness. Controlled polling, stopped-Indexer Playwright observation, stale/catch-up, and transaction-convergence scenarios passed.                                                                                                                                                                                                                                           |
| IDX-002  | Replay is idempotent and source/projection/checkpoint persistence is atomic                              | SQLite integration and real child-process pre/post-COMMIT `SIGKILL` scenarios passed.                                                                                                                                                                                                                                                                                                                                                                  |
| IDX-003  | Chain/checkpoint changes stop incremental ingestion and require the correct recovery path                | Batch seam tests reject a parent mismatch before commit. Real reorg tests detected noncanonical receipt evidence; database recovery retains orphan source events with per-row canonical, scan, and scope evidence, reindexes canonical state, and rejects an old deployment after `anvil_reset`.                                                                                                                                                       |
| IDX-004  | Rebuild derives state from verified source evidence and preserves catalog data                           | SQLite and real-stack rebuild/reconciliation scenarios passed. Contradictory source content requires a verified archive and scan-start reacquisition; a pure missing row remains reinsertable.                                                                                                                                                                                                                                                         |
| IDX-005  | A stricter indexing-depth policy cannot leave newer projected blocks marked current                      | Application regressions reject a checkpoint above the eligible target, keep unchanged-depth idle health, and catch forward when depth decreases. Explicit reindex remains the convergence path.                                                                                                                                                                                                                                                        |
| REC-001  | Projection and chain are compared at the same block/hash                                                 | Anchored sale/token enumeration preserves collection identity and detects missing, extra, wrong-collection, or wrong-owner rows; full claim-set comparison detects missing, changed, and extra rows; a later head remains `MATCH` plus `PROJECTION_LAGGING`; a lost anchor becomes `UNVERIFIABLE`.                                                                                                                                                     |
| ARCH-001 | Frontend and service dependencies follow declared public boundaries                                      | Architecture checks passed for the source graph and rejected invalid fixtures, including recovery-to-wallet-write and Web imports whose relative or aliased target resolves into the database package.                                                                                                                                                                                                                                                 |
| REL-001  | Generated ABI, OpenAPI, schema, projector, and deployment identities remain compatible                   | Generation checks, workspace consumers, schema source digest, runtime gates, API manifest/database descriptor gate, and same-address/new-deployment rejection passed locally.                                                                                                                                                                                                                                                                          |

## R7 recovery and concurrency coverage

- **TX-002:** the final live-context check occurs after the awaited durable
  `AWAITING_WALLET` write. Per-operation revisions reject stale journal updates, and Chromium
  verifies multi-tab writer handoff without using wall-clock order.
- **IDX-003:** an interrupted reindex resumes the target stored in its marker even after the head
  advances. Anchor change, mismatched recovery identity, and malformed marker cases fail closed and
  release their maintenance locks.
- **IDX-004:** any scope, decoder, or source-digest refresh requires a verified environment backup
  recorded in the marker before active raw events or headers can be removed. The archive fixture
  proves displaced source evidence remains readable.

Database acceptance IDs `DB-01` through `DB-75` are maintained in the
[database acceptance matrix](database-acceptance-matrix.md). Documentation acceptance IDs
`DOC-01` through `DOC-32` are maintained in the
[documentation acceptance matrix](documentation-acceptance-matrix.md).

## R14 source identity and long-running recovery coverage

- **IDX-003:** `IDX-026` uses an Anvil snapshot/revert at the same height to prove that logs are fetched
  against the post-reorg header hash before a scan-complete block is committed.
- **IDX-004:** reindex can exceed 1,000 successful batches, stops on successful no-progress responses,
  and resumes a durable `CATCHING_UP` marker without repeating its source rewind.

## R24 deployment proof and live-current coverage

- **TX-001 / REL-001:** pre-submit regression tests compare the immutable intent with the public
  RPC and wallet provider deployment identities. Any mismatch or unavailable proof stops before
  the wallet write; matching evidence permits one submission.
- **IDX-001:** integration regression tests keep a fixed-target catch-up below the live eligible
  head from publishing `CURRENT` and permit `CURRENT` only after live eligible convergence.
- **REL-001:** API tests allow the one configured frontend origin and reject a different local
  hostname under the default CORS policy.

## R25 recovery and bootstrap evidence coverage

- **IDX-003 / IDX-004:** persisted `RECOVERY_REQUIRED` survives normal writer startup, stale/current
  transitions, and ordinary commit attempts. Canonical/source reasons reject rebuild before marker
  publication; reindex retains the reason through replay and releases it only after target proof.
  Projector-only rebuild remains eligible after local source verification.
- **DB-30 / DB-71:** receipt JSON must have the expected schema and environment/deployment identity.
  Publication preserves a validated historical final receipt, rejects malformed or conflicting
  finals, and leaves no temporary file after success. Standard backup and later verification both
  validate receipt content.
- **IDX-001:** local-only journal rebuild leaves the last successful RPC and worker heartbeat times
  untouched, so observation freshness can remain stale despite successful projection replay.

## Public-chain readiness local coverage

- **SALE-001 / SALE-002:** Foundry and local browser scenarios cover reserved-buyer listing, outsider rejection, and exact-value funding by the selected buyer.
- **TX-001:** Browser tests keep receipt inclusion and finality separate; automatic observation may reuse durable finality proof, while manual recheck reads the RPC.
- **IDX-001 / IDX-003:** Profile, provider-capability, and finality tests keep unfinalized public blocks outside the projection. A real SQLite source snapshot is compared with a controlled secondary provider, and the audit CLI reports non-match outcomes with a nonzero exit.
- **REL-001:** Generated ABI, API, and database contracts carry the reserved-buyer field separately from the actual funding buyer.

All public-profile checks use mocked or loopback providers. No public RPC or deployment was tested.

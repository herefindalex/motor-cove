# 資料庫接受矩陣

[English](database-acceptance-matrix.md) · [简体中文](database-acceptance-matrix.zh-CN.md)

此矩陣保留資料庫切換 ID。僅噹噹前測試滿足規定要求時才使用 `executed/pass`。部分源或更窄的測試仍然是 `not run / partial`。

| 身分證號 | 需求總結                                   | 實施證據                                                                                                                 | 驗證                                                     |
| -------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- |
| DB-01    | 來自一份遷移歷史的新資料庫                 | Drizzle遷移+模式合約                                                                                                     | 執行/通過：`test:migrations`                             |
| DB-02    | 重複遷移是資料保留無操作                   | `migrateEnvironment` 前綴檢查                                                                                            | 執行/通過：`test:migrations`                             |
| DB-03    | 使用目錄/綁定升級先前的架構                | 真實的 `0000` 到 `0001` 夾具保留現有的鏈事件，同時添加可為空的來源記錄完整性證據；在發布當前摘要之前重新索引重新獲取來源 | 執行/通過：`test:migrations` 和 Indexer 商店集成         |
| DB-04    | 注入迁移 SQL 失败具有正确的事务/标记边界   | 可注入执行器在真实迁移事务中运行失败的 SQL；部分 DDL 回滚，外部标记保留 `FAILED` 和原始诊断                              | 執行/通過：`test:migrations`                             |
| DB-05    | 偵測已變更、遺失或未知的應用程式歷史記錄   | 有序歷史驗證器拒絕被篡改、遺失/落後和未知的帳本條目                                                                      | 執行/通過：`test:migrations`                             |
| DB-06    | 沒有遷移的架構變更失敗儲存庫檢查           | 產生的模式契約綁定排序模式來源檔案的摘要； `db:check` 拒絕來源/合約漂移                                                  | 執行/通過：`test:migrations` 和 `db:check`               |
| DB-07    | 即時缺少索引/約束/列是漂移                 | 標準化模式指紋                                                                                                           | 執行/通過：刪除索引測試                                  |
| DB-08    | 啟動時模式前/後失敗                        | 讀取器/寫入器使用的執行時間驗證器拒絕其本機遷移分類帳位於代碼合約後面的資料庫                                            | 執行/通過：`test:migrations` 和運行時讀取器邊界測試      |
| DB-09    | 遷移後崩潰，標記清除之前恢復，無需重新運行 | `recoverEnvironment`驗證                                                                                                 | 執行/通過：`test:migrations`                             |
| DB-10    | 大於 2^53 往返的值                         | 規範文字+閱讀器                                                                                                          | 執行/通過：`test:db`                                     |
| DB-11    | uint256 最大接受；無效表格被拒絕           | 編解碼器 + SQL 檢查                                                                                                      | 執行/通過：`test:db`                                     |
| DB-12    | 跨部署的相同銷售 ID 是隔離的               | 複合身分                                                                                                                 | 執行/通過：`test:db`                                     |
| DB-13    | 原始 SQL 狀態無效/FK/重複被拒絕            | SQL 約束/FK/PK                                                                                                           | 執行/通過：`test:db`                                     |
| DB-14    | API無法寫入或DDL                           | API仅接收读卡器表面；它的 SQLite 连接是只读的，直接 DDL 尝试被拒绝                                                       | 執行/通過：`test:db` 和架構檢查                          |
| DB-15    | 丢失元数据不会阻止摄取，并且 API 标记丢失  | 投影行在沒有目錄元資料和回應報告 `metadataStatus=MISSING` 的情況下保持可讀                                               | 執行/通過：`test:db`                                     |
| DB-16    | 不安全區塊/索引被拒絕                      | 安全整數檢查                                                                                                             | 執行/通過：`test:db`                                     |
| DB-17    | 第二個Indexer無法取得寫入器鎖              | `flock`寫鎖                                                                                                              | 執行/通過：`test:db`                                     |
| DB-18    | 運行時阻止遷移/恢復                        | 共享服務門與獨佔服務門                                                                                                   | 執行/通過：`test:db`                                     |
| DB-19    | 維護會阻止新的運作時間                     | 專屬                                                                                                                     | 執行/通過：`test:db`                                     |
| DB-20    | 包裝器/運行時崩潰正確釋放鎖                | Harness 擁有的子程序持有運行時/寫入器鎖，接收 `SIGKILL`，關閉其生命週期管道，並且新所有者獲取兩個鎖                      | 執行/通過：`test:db`                                     |
| DB-21    | 崩潰標記阻止運行時間                       | 外部標記檢查                                                                                                             | 執行/通過：`test:db`                                     |
| DB-22    | 繁忙超時/磁碟錯誤失敗但不跳過工作          | 受控`SQLITE_BUSY`保留阻塞值；透過 `max_page_count` 有界 `SQLITE_FULL` 回滾整個事務                                       | 執行/通過：`test:db`；未聲明主機檔案系統耗盡             |
| DB-23    | 相同目錄種子重新運行是無操作的             | 交易平等檢查                                                                                                             | 執行/通過：`test:seeds`                                  |
| DB-24    | 手動目錄編輯不會被覆蓋                     | `SEED_CONFLICT` 並回滾                                                                                                   | 執行/通過：`test:seeds`                                  |
| DB-25    | 跨部署的相同令牌獨立綁定                   | 複合結合PK                                                                                                               | 執行/通過：`test:seeds`                                  |
| DB-26    | 已完成的鏈種子不會重新發送                 | Bootstrap 將每個鏈步驟保留為 `PREPARED`、`SUBMITTED`，然後是 `VERIFIED`；完成重新運行驗證並重複使用現有清單              | 執行/通過：日誌單元案例和實際堆疊重新運行                |
| DB-27    | 廣播中斷已驗證或未知                       | 日誌恢復已知提交的雜湊值，重新驗證已驗證的收據，並針對不明確的提交或擱淺的 `PREPARED` 步驟停止 `UNKNOWN`                 | 對於確定性狀態機情況執行/通過；真正的進程終止視窗未執行  |
| DB-28    | 重新執行引導程式不會反轉使用者操作         | 真實堆疊測試完成使用者結算和恢復操作，重新運行引導程序，並斷言區塊高度、計數器、所有者、清單、日誌和投影不變             | 執行/通過：真實堆疊重新運行                              |
| DB-29    | 種子版本/清單不符停止                      | 應用的目錄種子版本 `1` 拒絕版本 `2` 和 `SEED_CONFLICT` 並保留原始資料集                                                  | 執行/通過：`test:seeds`                                  |
| DB-30    | Bootstrap 透過目標區塊/雜湊斷言完成        | Bootstrap捕獲目標，追上，驗證規範哈希，寫入收據；真實堆疊設定和重新運行練習都創建和重用路徑                              | 執行/通過：真實堆疊引導並重新運行                        |
| DB-31    | 演示狀態來自真實事件                       | 真實的 Anvil 生命週期場景已通過                                                                                          | 執行/通過：真實堆疊生命週期                              |
| DB-32    | Event/投影/header/checkpoint一起回滾       | SQLite適配器事務回滾測試                                                                                                 | 執行/通過：Indexer 商店集成                              |
| DB-33    | 零事件區塊推進完整的檢查點                 | 使用檢查點提交的已完成區塊記錄和摘要                                                                                     | 執行/通過：Indexer 商店集成                              |
| DB-34    | 同塊重播是經過內容驗證的無操作             | 適配器驗證儲存的事件/區塊內容                                                                                            | 執行/通過：Indexer 商店集成                              |
| DB-35    | 相同的事件標識/不同的內容是完整性錯誤      | 適配器拒絕相互矛盾的原始證據                                                                                             | 執行/通過：Indexer 商店集成                              |
| DB-36    | 新發現的登入已完成的舊區塊需要重新索引     | 已完成的區塊內容摘要拒絕新發現的帶有 `COMPLETED_BLOCK_CONTENT_MISMATCH` 的日誌                                           | 執行/通過：Indexer 商店集成                              |
| DB-37    | 投影器版本不匹配會阻止增量攝取             | 商店啟動比較持久化的投影器合約並拒絕與 `PROJECTION_CONTRACT_MISMATCH` 不匹配                                             | 執行/通過：Indexer 商店集成                              |
| DB-38    | 損壞的投影從所有原始事件重建               | 原子本地-日誌重建                                                                                                        | 執行/通過：Indexer 商店集成                              |
| DB-39    | 失敗的重建永遠不會暴露半建置；目錄已儲存   | 重建事務和目錄保存斷言                                                                                                   | 執行/通過：Indexer 商店集成                              |
| DB-40    | Reorg reindex 產生規範結果和可追溯的舊證據 | 真正的Anvil快照/恢復孤兒資助事件；重新索引恢復規範的 `LISTED` 狀態，同時保留非規範區塊下的原始事件                       | 已執行/通過：真正的資料庫復原集成                        |
| DB-41    | 偵測到重複使用鏈/位址的 Anvil 重設         | 在 `anvil_reset` 之後，確定性位址在新的部署 ID 下重新部署，且舊環境拒絕使用 `DEPLOYMENT_MISMATCH` 重新索引               | 已執行/通過：真正的資料庫復原集成                        |
| DB-42    | 除了操作字段之外，兩個重建在語義上是相同的 | 真實堆疊測試根據相同的經過驗證的證據重建兩次，並比較銷售和車輛結果                                                       | 執行/通過：實棧恢復                                      |
| DB-43    | 在 H 處比較，而頭 H+k 給出 MATCH + LAGGING | 真實堆疊測試挖掘稍後的頭塊並在錨定檢查點斷言 `MATCH` 和 `PROJECTION_LAGGING`                                             | 執行/通過：實棧恢復                                      |
| DB-44    | 透過完整性範圍偵測缺少的整個銷售           | 協調器錨定 `saleCount` 並檢查 `[1, saleCount]` 中的每個銷售 ID                                                           | 執行/通過：實棧恢復                                      |
| DB-45    | 缺少歷史狀態/更改錨點是無法驗證的          | Anvil快照/恢復刪除索引錨點；協調器持續存在 `UNVERIFIABLE` 錯誤並退出非零                                                 | 執行/通過：實棧恢復                                      |
| DB-46    | 後來的 NFT 轉移推動了當前所有者的比較      | 協調器透過錨定的 `nextTokenId` 列舉每個鑄造的代幣，比較 `ownerOf`，並偵測完全遺失的所有權行                              | 執行/通過：實棧恢復                                      |
| DB-47    | WAL 提交的資料在獨立快照復原中仍然存在     | SQLite 備份 API                                                                                                          | 執行/通過：`test:recovery`                               |
| DB-48    | 損壞的校驗和/元資料在主動突變之前被拒絕    | 備份驗證器                                                                                                               | 執行/通過：校驗和情況                                    |
| DB-49    | 頭部後面恢復的相同部署可以趕上             | 一致的預資助備份恢復舊的檢查點，相同的 Indexer 趕上未更改的即時部署，`FUNDED` 投影                                       | 已執行/通過：真正的資料庫復原集成                        |
| DB-50    | 恢復檔案切換崩潰是可診斷/可恢復的          | 標記恢復隔離主/WAL/SHM 代                                                                                                | 執行/通過：殺死熱 WAL 開關；未聲明硬體斷電               |
| DB-51    | 重置拒絕外部節點和主動引導程式所有權       | Reset 拒絕錯誤的 RPC 身分和 `anvil_reset` 之前的引導鎖                                                                   | 執行/通過：重置 CLI 生命週期矩陣；環回模擬未在本地聲明。 |
| DB-52    | 重置保留鎖定 inode 和備份                  | 重置測試保留擁有者標記、鎖定索引節點和備份，同時刪除產生的執行時間狀態                                                   | 執行/通過：`test:db`                                     |
| DB-53    | API資料/來源共享一讀快照                   | 第二個寫入者在讀取屏障處提交完成；飛行中的觀察仍然返回早期的銷售、檢查點和活動快照                                       | 執行/通過：Indexer 商店集成                              |
| DB-54    | 並行套件隔離資料庫/鏈/隨機數/連接埠/鎖     | 真實堆疊、事務融合和資料庫復原套件使用單獨的根、資料庫、連接埠、鎖定、部署和簽署者隨機數空間                             | 執行/通過：三個真實套件與`--maxWorkers=3`同時運行        |

目前執行的資料庫記錄在`docs/evidence/verification.json`中。此矩陣不會將較窄的測試轉換為完整項目的測試。
| DB-55 | 中斷的遷移僅在記錄的捆綁包標識下恢復或完成 | 失敗的已知前綴仍然需要執行操作，不匹配的捆綁包將被拒絕，並且匹配的遷移將繼續，並且當前的 SQL 在標記刪除之前最終確定 `db_contract` | 執行/通過：`test:migrations` |
| DB-56 | 來源不完整重建僅轉換為完整的身份綁定重新索引 | 拒絕選擇加入和不匹配的掃描開始；明確掃描開始轉換以取得新的操作 ID 並記錄失敗的重建沿襲 | 執行/通過：投影維護測試 |
| DB-57 | 遷移備份保留有效的預部署資料庫 | 零部署且無部署 sidecar 的真正舊模式資料庫可產生經過驗證的 `PREDEPLOYMENT` 備份、保留目錄資料並在無需偽造鏈身分的情況下進行遷移 | 執行/通過：`test:migrations` 和 `test:recovery`；在主動替換之前直接恢復被拒絕 |
| DB-58 | 託管本地重置僅限於一個專用節點 | Bootstrap 自動宣告一個標準化的環回端點；索引器啟動和重置驗證綁定；重置會保留它；單位固定裝置涵蓋別名、衝突、變更和所有權缺失 | 執行/通過：資料庫單元測試和Anvil `managed-node-reset` 的兩個整合；未知進程和轉送別名仍然在宣告模型之外 |
| DB-59 | 哈希後種子日誌寫入故障恢復，無需重新提交 | 一次注入的 `SUBMITTED` 寫入失敗將被重試，作為持久的已知哈希證據；重新打開的日誌驗證哈希值，同時提交計數仍然為 1 | 執行/通過：鏈種子日誌單元回歸 |
| DB-60 | 第一次對帳磁頭故障建立持久的故障報告 | 真實記憶體中的 SQLite 收到新的 `UNVERIFIABLE / HEAD_UNKNOWN` 報告，其中包含檢查點身分和 RPC 原因； writer 關閉且退出狀態非零 | 執行/通過：對帳報告生命週期回歸 |
| DB-61 | 對帳保留 NFT 收藏身份 | 清單集合行滿足錨定所有權；錯誤集合行是明確的 `UNEXPECTED_COLLECTION` 不匹配，即使在正確行旁邊也是如此；其他部署保持隔離 | 執行/通過：重點SQLite/CLI案例和真實Anvil/SQLite 對帳 |
| DB-62 | 重新取得之前將矛盾的來源內容存檔 | 非空事件/標頭摘要不匹配需要經過驗證的完整源刷新；備份保留舊證據，重新獲取活動鏈源，保留目錄；純缺失行使用倒回/重新插入 | 執行/傳遞：Indexer儲存和真實Anvil恢復集成 |
| DB-63 | 在 SQL 之前重試失敗的遷移前備份 | 匹配沒有備份證明的失敗標記會重新執行真正的備份驗證，並在重複失敗時保持模式/歷史不變 | 執行/通過：`test:migrations` |
| DB-64 | 已完成的遷移前備份被重新驗證並重複使用 | 標記保留已發布的備份 ID；匹配簡歷驗證來源/部署/架構證據，並在註入 SQL 失敗後使用快照 | 執行/通過：`test:migrations` |
| DB-65 | 不採用現有的無主資料庫 | 初始化程序和遷移在所有者創建之前拒絕並保留原始資料庫字節 | 執行/通過：`test:migrations` |
| DB-66 | 不採用現有的無主 sidecar | 部署、引導接收、種子日誌、維護和託管節點 sidecar 保持位元組相同，無需所有者元數據 | 執行/通過：`test:migrations` |
| DB-67 | 空環境所有權仍然是冪等的 | 空目錄接收一條所有者記錄而不建立資料庫；重新運行保留所有者位元組 | 執行/通過：`test:migrations` |

| DB-68 | 孤立標記臨時文件不會束縛匹配的簡歷 | 一個真正的孩子沖掉了以前的固定名稱臨時標記並被殺死；發布的 `PREPARED` 標記仍然具有權威性，相同的操作運行一次，孤兒被刪除 | 執行/通過：`test:db` |
| DB-69 | 標準備份拒絕不完整的維護來源 | 失敗的重新索引標記保持位元組相同，並且不會發布備份包 | 執行/通過：`test:recovery` |
| DB-70 | 維護檔案保留原始狀態，無法正常恢復 | 一次來源刷新歸檔記錄操作且投影證據為`EVIDENCE_ONLY`；驗證成功並恢復隔離前拒絕 | 執行/通過：`test:recovery` |
| DB-71 | 備份和復原不能跨越不完整的引導生成 | 共用/獨佔引導程式所有權區塊重疊；不完整或變更的種子日誌/收據證據在備份發布或復原隔離之前被拒絕 | 執行/通過：恢復和遷移測試 |
| DB-72 | 對帳新鮮度使用發表時間頭 | 受控頭部在錨定比較期間前進並產生 `MATCH / PROJECTION_LAGGING`；出版頭故障產生耐用的 `UNVERIFIABLE / HEAD_UNKNOWN` | 執行/通過：對帳生命週期集成 |
| DB-73 | 重置意圖先於鏈突變，恢復使用重置階段證據 | `RESET / PREPARED`在鏈回調之前是持久的；進程死亡仍然可操作； `CHAIN_RESET` 恢復完成生成狀態清理而不祝福舊資料庫 | 已執行/通過：重置資料庫、CLI、child-kill 和託管節點整合測試 |
| DB-74 | 重置拒絕在鏈突變之前替換破壞性子路徑 | 維護所有權後，`databaseDir` 和 `reportsDir` 必須保留真實環境根目錄包含的非符號連結目錄；以外部符號連結取代會使鏈回呼、標記和外部位元組保持不變 | 執行/通過：`test:db` 中的真實臨時檔案系統和符號連結回歸 |
| DB-75 | 恢復在活動隔離之前重新驗證複製的快照位元組 | 複製後，暫存資料庫 SHA-256 仍必須與驗證的備份清單相符；在活動主/WAL/SHM 移動之前驗證/複製來源變更失敗 | 執行/通過：`test:recovery`中真實的better-sqlite3備份/恢復回歸 |
